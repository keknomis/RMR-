<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="footballApp.Pages.Login"
             Title="Login">
    <Grid Margin="16" BackgroundColor="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0">
            <Label TextColor="#006385" HorizontalTextAlignment="Center" FontSize="20" Text="LOGIN" />
        </StackLayout>
        <StackLayout Grid.Row="1">
            <Label TextColor="#006385" Text="Email:" />
            <Entry x:Name="Email" Placeholder="Email" />
            <Label TextColor="#006385" Text="Password:" />
            <Entry x:Name="Password" Placeholder="Password" IsPassword="True" />
        </StackLayout>
        <Button Grid.Row="2" x:Name="btnLogin" Text="Login" TextColor="White" BackgroundColor="#006385" Clicked="LoginClicked" />
    </Grid>
</ContentPage>